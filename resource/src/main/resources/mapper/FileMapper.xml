<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lk.modules.mapper.FileMapper">
    <insert id="saveFile">
        insert into t_file(fileName, fileUrl, fileSize, fileType)
        values (#{fileName}, #{fileUrl}, #{fileSize}, #{fileType})
    </insert>
</mapper>